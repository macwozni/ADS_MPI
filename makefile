# dummy makefile - the real one is in make-shared/

# following local files used by makefile are user modifiable:

# m_options: specifies compiler specific options (this file is
#    initialized from make-shared/m_compile)

# m_files: lists all source files and project specific options

include makefile-common
