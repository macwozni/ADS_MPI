# Backward-compatible entry point for scripts that still use
# CONFIG=mymake/m_options. The canonical editable file is repository-root
# m_options.
_ADS_MYMAKE_CONFIG_DIR := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
include $(_ADS_MYMAKE_CONFIG_DIR)../m_options
