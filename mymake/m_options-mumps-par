# Legacy compatibility alias. New selectable examples live in makeconfig/;
# all local paths and MUMPS settings are centralized in repository-root
# m_options.
_ADS_LEGACY_CONFIG_DIR := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
include $(_ADS_LEGACY_CONFIG_DIR)../m_options
